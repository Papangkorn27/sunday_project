[project]
name = "sunday-project-p2"
version = "0.1.0"
description = "Sunday data engineer assignment Part 2 ETL and Airflow bonus implementation."
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2.3",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
